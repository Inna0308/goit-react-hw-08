import styles from "./HomePage.module.css";

const HomePage = () => {
  return (
    <div className={styles.homePage}>
      <h4 className={styles.homePageText}>Save your contacts!</h4>
    </div>
  );
};

export default HomePage;
